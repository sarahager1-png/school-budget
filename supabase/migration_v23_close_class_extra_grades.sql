-- v23 — אפשרות לבחור שכבות נוספות להצעת "סגירת כיתה"
--
-- closeClassReport תמיד הציעה לסגור רק את הכיתה בשכבה הגבוהה ביותר בבית
-- הספר (בכוונה — כדי לא להציע לסגור שכבת ביניים בזמן שהגבוהה ממנה עוד
-- פתוחה). בבתי ספר עם כמה שכבות רצופות בתפוסה נמוכה (למשל ירושלים: ג/ד/ה)
-- המנהלת רוצה לראות כל שכבה כזו כהצעה נפרדת לבחירה, לא רק את הגבוהה.
--
-- עמודה זו (NULL כברירת מחדל) מחזיקה רשימת אותיות שכבה מופרדות בפסיק
-- (למשל "ג,ד") שנוספות כמועמדות, בנוסף לשכבה הגבוהה שנבדקת תמיד. כל עוד
-- לא נקבע ערך — ההתנהגות זהה לחלוטין להיום בכל בתי הספר.
--
-- החלה:  node scripts/apply-sql.mjs <ref> <tokenFile> supabase/migration_v23_close_class_extra_grades.sql

alter table public.financial_constants
  add column if not exists close_class_extra_grades text;

comment on column public.financial_constants.close_class_extra_grades is
  'שכבות נוספות (מלבד הגבוהה ביותר) שמוצעות בנפרד לסגירה, כאותיות מופרדות בפסיק (למשל "ג,ד"). NULL = רק השכבה הגבוהה, כמו קודם.';
