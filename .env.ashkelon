# שלהבות אשקלון — build config (loaded by `vite build --mode ashkelon`)
# The anon/publishable key is safe to commit (it ships in the client bundle anyway).
# The service_role key is NOT here — it lives in .env.ashkelon.local (gitignored).
VITE_SUPABASE_URL=https://ogkwvrerolofujhydhsl.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Y_jPOeoCpSwJt60TugZmmg_63jvcRvq
VITE_SCHOOL_NAME=שלהבות אשקלון
