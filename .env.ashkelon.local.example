# Copy to `.env.ashkelon.local` (gitignored) and fill in.
# Used ONLY by scripts/seed-admin.mjs — never shipped to the browser.
SUPABASE_SERVICE_ROLE_KEY=__ASHKELON_SERVICE_ROLE_KEY__

# Optional admin-login overrides (defaults shown):
# ADMIN_EMAIL=data@reshetch.org.il
# ADMIN_PASSWORD=ashkelon2026
# ADMIN_NAME=מנהלת הרשת
# ADMIN_ROLE=admin
